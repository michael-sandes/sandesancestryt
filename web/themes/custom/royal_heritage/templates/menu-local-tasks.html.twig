{#
/**
 * @file
 * Royal Heritage theme: menu-local-tasks.html.twig
 *
 * Wraps the primary (and optional secondary) local task tab lists.
 *
 * Available variables:
 *   - primary   : render array of primary local task items
 *   - secondary : render array of secondary local task items
 */
#}
{% if primary %}
  <nav class="rh-tabs" aria-label="{{ 'Page actions'|t }}">
    <ul class="rh-tabs__list rh-tabs__list--primary">
      {{ primary }}
    </ul>
  </nav>
{% endif %}

{% if secondary %}
  <nav class="rh-tabs rh-tabs--secondary" aria-label="{{ 'Secondary page actions'|t }}">
    <ul class="rh-tabs__list rh-tabs__list--secondary">
      {{ secondary }}
    </ul>
  </nav>
{% endif %}
